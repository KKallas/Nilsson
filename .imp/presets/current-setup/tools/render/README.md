# Render tools

Wrapper tools that expose renderers (mermaid, plotly, gantt, etc.) as regular tool scripts. The agent can call these the same way as any other tool.

Each script generates a chart/diagram URL that the chat UI renders inline.
